# Task Completion Checklist

After completing any code changes:

1. Run `pnpm run validate` (lint + format + typecheck)
2. If format fails, run `pnpm run format:fix` and re-validate
3. No test runner is configured — visual verification may be needed
4. Commit only when explicitly asked by the user
